{
  "name": "donichannel",
  "version": "0.1.0",
  "private": true,
  "description": "DoNiChannel Tauri desktop app",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "frontend:dev": "npm run dev --prefix ./ui",
    "frontend:build": "npm run build --prefix ./ui",
    "backend": "python main.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}